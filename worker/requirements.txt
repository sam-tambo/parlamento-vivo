# Core transcription
openai-whisper>=20231117

# Supabase (optional — worker uses minimal urllib instead, but useful for tooling)
supabase>=2.0.0

# Stream acquisition
streamlink>=6.0.0
yt-dlp>=2024.1.0

# Browser automation (Playwright — for scraping JavaScript-rendered ARTV pages)
playwright>=1.40.0
# After install run: playwright install chromium

# Speaker diarization (OPTIONAL — requires HF_TOKEN + accepted model licenses)
# https://huggingface.co/pyannote/speaker-diarization-3.1
# https://huggingface.co/pyannote/embedding
pyannote.audio>=3.1.0
torch>=2.0.0
torchaudio>=2.0.0
scipy>=1.10.0

# Audio processing
ffmpeg-python>=0.2.0

# Deputy scraping (scrape_deputados.py)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Text similarity (dar_profiles.py)
# difflib is stdlib — no extra install needed
